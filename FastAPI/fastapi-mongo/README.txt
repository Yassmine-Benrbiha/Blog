$ cd fastapi-mongo
$ py -m venv venv
$ source venv/bin/activate //or root to activate file under venv folder it could be different
$ export PYTHONPATH=$PWD 
(venv)$ pip install -r requirements.txt

(venv)$ python app/main.py ==> Now you are ready open http://localhost:7000/blog/